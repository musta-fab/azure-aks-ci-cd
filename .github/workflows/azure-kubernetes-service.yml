name: Deploy to AKS

on:
  push:
    branches: [ "main" ]

env:
  IMAGE_NAME: web

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    # Login to Azure
    - uses: azure/login@v2
      with:
        creds: ${{ secrets.SP {
  "clientId": "584274aa-c12e-4961-812d-ec5ee92ad280",
  "clientSecret": "lNY8Q~yYTrKQQl3.H_p7G9di.pTjNRYMVDUInatw",
  "subscriptionId": "81652094-be63-4fb4-870b-c326131700ef",
  "tenantId": "9d35ce4d-bf3a-4ae5-b6d3-6c1faf8c3d53",
  "activeDirectoryEndpointUrl": "https://login.microsoftonline.com",
  "resourceManagerEndpointUrl": "https://management.azure.com/",
  "activeDirectoryGraphResourceId": "https://graph.windows.net/",
  "sqlManagementEndpointUrl": "https://management.core.windows.net:8443/",
  "galleryEndpointUrl": "https://gallery.azure.com/",
  "managementEndpointUrl": "https://management.core.windows.net/"
} }}

    # Login to ACR
    - run: az acr login --name ${{ secrets.mustaacr }}

    # Build & Push (amd64 enforced)
    - name: Build and Push
      run: |
        IMAGE=${{ secrets.mustaacr }}.azurecr.io/${{ env.IMAGE_NAME }}:${{ github.sha }}
        docker build --platform linux/amd64 -t $IMAGE .
        docker push $IMAGE
        echo "IMAGE=$IMAGE" >> $GITHUB_ENV

    # Set AKS context
    - uses: azure/aks-set-context@v4
      with:
        resource-group: ${{ secrets.6pm-rg }}
        cluster-name: ${{ secrets.6pmk8s }}

    # Deploy
    - name: Deploy
      run: |
        sed "s|IMAGE|$IMAGE|g" k8s/deployment.yaml | kubectl apply -f -
